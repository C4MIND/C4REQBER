apiVersion: v1
kind: Service
metadata:
  name: c4reqber-api
  namespace: c4reqber
  labels:
    app: c4reqber-api
spec:
  type: ClusterIP
  selector:
    app: c4reqber-api
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP